{
  "name": "paynoval-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:fetch": "node scripts/fetch-specs.js",
    "docs:lint": "redocly lint specs/*.yaml",
    "docs:build": "npm run docs:fetch && redocly build-docs specs/gateway.yaml -o public/gateway.html --title=\"PayNoval Gateway API\" && redocly build-docs specs/internal.yaml -o public/internal.html --title=\"PayNoval Internal API\"",
    "docs:serve": "http-server public -p 5050 -c-1"
  },
  "devDependencies": {
    "@redocly/cli": "^1.16.0",
    "http-server": "^14.1.1"
  }
}

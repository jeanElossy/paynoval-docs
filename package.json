{
  "name": "paynoval-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:fetch": "node scripts/fetch-specs.js",
    "docs:lint": "redocly lint specs/*.yaml",
    "docs:build": "redocly build-docs specs/gateway.yaml -o public/gateway.html --config redocly.yaml --title=\"PayNoval Gateway API\" && redocly build-docs specs/internal.yaml -o public/internal.html --config redocly.yaml --title=\"PayNoval Internal API\"",
    "docs:serve": "http-server public -p 5050 -c-1",
    "docs:preview": "redocly preview-docs specs/gateway.yaml --port 5051 --config redocly.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "^2.5.1",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
